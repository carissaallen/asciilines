# Read .tvg file.
# First line contains the number of rows and columns for text to output.
# Original grid should contain dots.
# 1: character to render (overwrite dot).
# 2: row position to start (0 based).
# 3: column position to start (0 based).
# 4: h for horizontal line, v for vertical line.
# 5: length of the line ( 0 < line <= column, row)
